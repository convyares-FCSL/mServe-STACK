<root BTCPP_format="4">
  <BehaviorTree ID="parallel_high">
    <BoostHigh
      action_name="{high_booster_action}"
      command="{command}"
      target_pressure="{target_pressure}"
      cpm="{cpm}"
      speed_rpm="{speed_rpm}"
    />
  </BehaviorTree>
</root>
